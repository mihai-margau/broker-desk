"use server";

import CreateRequest from "../../../components/CreateRequest";

export default async function Create() {
 
  return (
    <main>
      <div>
        
        <CreateRequest /> 
    
      </div>
    </main>
  );

  // Optional: robust unescape helper (safer than manual replace)

}